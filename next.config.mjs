/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // NOTE: Images are served as plain <img> by default in this template to avoid config friction.
  // If you switch to next/image for external hosts, configure images.remotePatterns here.
};

export default nextConfig;
